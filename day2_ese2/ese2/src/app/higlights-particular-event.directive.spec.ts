import { HiglightsParticularEventDirective } from './higlights-particular-event.directive';

describe('HiglightsParticularEventDirective', () => {
  it('should create an instance', () => {
    const directive = new HiglightsParticularEventDirective();
    expect(directive).toBeTruthy();
  });
});
